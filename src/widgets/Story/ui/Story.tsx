import { Section, SectionTitle } from "@/shared/ui";

/** Секция «Story» макета — Figma node 114:2511 */
export function Story() {
  return (
    <Section id="story" className="py-20 lg:py-30">
      <SectionTitle>Story</SectionTitle>
    </Section>
  );
}
