import { Section, SectionTitle } from "@/shared/ui";

/** Секция «Events» макета — Figma node 114:2549 */
export function Events() {
  return (
    <Section id="events" className="py-20 lg:py-30">
      <SectionTitle>Events</SectionTitle>
    </Section>
  );
}
