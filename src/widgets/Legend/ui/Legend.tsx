import { Section, SectionTitle } from "@/shared/ui";

/** Секция «Legend» макета — Figma node 114:2414 */
export function Legend() {
  return (
    <Section id="legend" className="py-20 lg:py-30">
      <SectionTitle>Legend</SectionTitle>
    </Section>
  );
}
