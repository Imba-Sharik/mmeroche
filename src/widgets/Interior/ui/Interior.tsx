import { Section, SectionTitle } from "@/shared/ui";

/** Секция «Interior» макета — Figma node 114:2483 */
export function Interior() {
  return (
    <Section id="interior" className="py-20 lg:py-30">
      <SectionTitle>Interior</SectionTitle>
    </Section>
  );
}
