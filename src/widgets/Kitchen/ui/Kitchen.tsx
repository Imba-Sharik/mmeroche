import { Section, SectionTitle } from "@/shared/ui";

/** Секция «Kitchen» макета — Figma node 114:2427 */
export function Kitchen() {
  return (
    <Section id="kitchen" className="py-20 lg:py-30">
      <SectionTitle>Kitchen</SectionTitle>
    </Section>
  );
}
