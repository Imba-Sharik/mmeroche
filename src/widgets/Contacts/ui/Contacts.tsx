import { Section, SectionTitle } from "@/shared/ui";

/** Секция «Contacts» макета — Figma node 114:2576 */
export function Contacts() {
  return (
    <Section id="contacts" className="py-20 lg:py-30">
      <SectionTitle>Contacts</SectionTitle>
    </Section>
  );
}
