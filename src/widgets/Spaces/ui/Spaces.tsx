import { Section, SectionTitle } from "@/shared/ui";

/** Секция «Spaces» макета — Figma node 114:2457 */
export function Spaces() {
  return (
    <Section id="spaces" className="py-20 lg:py-30">
      <SectionTitle>Spaces</SectionTitle>
    </Section>
  );
}
