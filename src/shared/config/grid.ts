/**
 * Метрики сетки из макета (1920w).
 * Значения продублированы CSS-переменными в globals.css (--container-inset, --grid-gap).
 */
export const GRID = {
  /** Ширина макета, к которой привязаны все размеры */
  designWidth: 1920,
  /** Горизонтальные поля контейнера */
  inset: 50,
  /** Гэп между колонками и рядами */
  gap: 20,
  /** Число колонок на десктопе */
  columns: 3,
  /** Hero по макету — на всю высоту вьюпорта, 1920×1080 */
  hero: { height: 1080 },
} as const;
