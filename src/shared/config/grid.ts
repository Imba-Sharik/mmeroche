/**
 * Метрики сетки из макета (Figma node 222:1967, 1920w).
 * Значения продублированы CSS-переменными в globals.css
 * (--container-max, --container-inset, --grid-gap).
 */
export const GRID = {
  /** Ширина макета, к которой привязаны все размеры */
  designWidth: 1920,
  /** Контейнер контента: 1464 при полях 228 по краям */
  container: 1464,
  /** Горизонтальные поля на узких экранах: 16 на мобильном, 20 от 640px */
  inset: { mobile: 16, sm: 20 },
  /** Гэп между колонками и рядами */
  gap: 32,
  /** Число колонок на десктопе */
  columns: 3,
  /** Hero по макету — на всю высоту вьюпорта, 1920×1080 */
  hero: { height: 1080 },
} as const;
